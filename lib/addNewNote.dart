import 'package:flutter/material.dart';
class Add_New_Note extends StatelessWidget {
  const Add_New_Note({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
